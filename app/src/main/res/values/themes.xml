<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Pockemons" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/orange</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/pockemons_back</item>
        <item name="postSplashScreenTheme">@style/Theme.Pockemons</item>
    </style>
</resources>